---
# Namespace for MongoDB Content Database
# This namespace isolates the MongoDB deployment and related resources
apiVersion: v1
kind: Namespace
metadata:
  name: content-db
  labels:
    app: mongodb
    environment: production
    purpose: content-database
  annotations:
    description: "Namespace for MongoDB flexible content database deployment (Movies, Books, Music)"

# Made with Bob
