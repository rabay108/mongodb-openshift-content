---
# ServiceAccount for MongoDB
# This service account will be used by the MongoDB pod
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mongodb-sa
  namespace: content-db
  labels:
    app: mongodb
    component: database
  annotations:
    description: "Service account for MongoDB StatefulSet with proper SCC permissions"

# Made with Bob